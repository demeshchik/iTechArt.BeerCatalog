\/**
 * Created by a.dziameshchyk on 10/24/2017.
 */

function pagination(totalCount, perPage, currentPage) {
    var totalPages = Math.ceil(totalCount / perPage);

    if (totalPages === 1)
        return;


};